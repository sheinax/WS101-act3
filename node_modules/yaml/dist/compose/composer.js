 Microsoft.Помощ от Microsoft EdgeИскате ли автоматично да влизате в сайтове на Microsoft с текущия си профил?Излезли сте от сайт на Microsoft преди. Изберете "Да" тук, за да влизате автоматично в сайтове на Microsoft, като Bing и Outlook, като използвате текущия си профил.Влезте отново в сайтовете на MicrosoftЗа да влезете в $1 и други сайтове на Microsoft, изберете "Влизане" отново.Влезте бързо в сайтове на Microsoft, като влезете в Microsoft EdgeАвтоматично влизане в сайтове на Microsoft, като например Bing и Outlook, чрез влизане в Microsoft Edge.Вашите предпочитани, пароли, хронология и други данни за сърфирането са налични на всички ваши устройства. Можете да изберете да излезете от $1.Сега сте влезли в Microsoft EdgeАвтоматично влизане в този сайт с този профил?Microsoft Edge ще използва този профил за автоматично влизане в този сайт при всяко посещение.Влезте като $1Променете предпочитанието си за профил под "Настройки", ако искате да пропуснете автоматичното влизане.УправлениеВашият профилВлезте в Microsoft Edge, влезте навсякъде.Автоматично влизане в този сайт и други сайтове на Microsoft, когато влизате в Microsoft Edge.Влезте в $1 с MicrosoftСъздаване на акаунт за $1 с MicrosoftИзползвайте $1 с MicrosoftПроверяване…Продължете като $1този уеб сайтПроверка с $1За да създадете своя акаунт, Microsoft ще сподели вашето име, имейл адрес и снимка на профила ви с $1. Можете да промените тези разрешения на $2.Поверителност & бисквиткиВключиОще действия{COUNT, plural,
      =1 {Вашият прозорец InPrivate няма да се отвори след повторно стартиране}
      one {Вашите {COUNT} прозореца InPrivate няма да се отворят отново след повторно стартиране}
      other {Вашите {COUNT} прозореца InPrivate няма да се отворят отново след повторно стартиране}}Версия $1 ($2) $3$4ДостъпностПревод на живоИзтегляне на файловете за разпознаване на говор… $1%ОбликВъвеждане на URL адресТемаПоказване на лентата с предпочитаниНова страница на разделГлавно менюТърсене на настройкиНастройкиОтидете в настройките на $1, за да промените товаПо изборПроверетеПроизходът трябва да е сигуренДобавяне на адресРедактиране на адресСтрана/регионТелефонИмейлИзтриване на адресРедактиране на картаЗа да добавите или управлявате методите си за плащане в Google Pay, посетете своя <a href="$1" target="_blank">акаунт в Google</a>Добавяне на картаИме на картатаНомер на картаДата на изтичане на валидносттаМесец на изтичане на валидносттаГ� errors: this.errors,
            warnings: this.warnings
        };
    }
    /**
     * Compose tokens into documents.
     *
     * @param forceDoc - If the stream contains no document, still emit a final document including any comments and directives that would be applied to a subsequent document.
     * @param endOffset - Should be set if `forceDoc` is also set, to set the document range end and to indicate errors correctly.
     */
    *compose(tokens, forceDoc = false, endOffset = -1) {
        for (const token of tokens)
            yield* this.next(token);
        yield* this.end(forceDoc, endOffset);
    }
    /** Advance the composer by one CST token. */
    *next(token) {
        if (process.env.LOG_STREAM)
            console.dir(token, { depth: null });
        switch (token.type) {
            case 'directive':
                this.directives.add(token.source, (offset, message, warning) => {
                    const pos = getErrorPos(token);
                    pos[0] += offset;
                    this.onError(pos, 'BAD_DIRECTIVE', message, warning);
                });
                this.prelude.push(token.source);
                this.atDirectives = true;
                break;
            case 'document': {
                const doc = composeDoc.composeDoc(this.options, this.directives, token, this.onError);
                if (this.atDirectives && !doc.directives.docStart)
                    this.onError(token, 'MISSING_CHAR', 'Missing directives-end/doc-start indicator line');
                this.decorate(doc, false);
                if (this.doc)
                    yield this.doc;
                this.doc = doc;
                this.atDirectives = false;
                break;
            }
            case 'byte-order-mark':
            case 'space':
                break;
            case 'comment':
            case 'newline':
                this.prelude.push(token.source);
                break;
            case 'error': {
                const msg = token.source
                    ? `${token.message}: ${JSON.stringify(token.source)}`
                    : token.message;
                const error = new errors.YAMLParseError(getErrorPos(token), 'UNEXPECTED_TOKEN', msg);
                if (this.atDirectives || !this.doc)
                    this.errors.push(error);
                else
                    this.doc.errors.push(error);
                break;
            }
            case 'doc-end': {
                if (!this.doc) {
                    const msg = 'Unexpected doc-end without preceding document';
                    this.errors.push(new errors.YAMLParseError(getErrorPos(token), 'UNEXPECTED_TOKEN', msg));
                    break;
                }
                this.doc.directives.docEnd = true;
                const end = resolveEnd.resolveEnd(token.end, token.offset + token.source.length, this.doc.options.strict, this.onError);
                this.decorate(this.doc, true);
                if (end.comment) {
                    const dc = this.doc.comment;
                    this.doc.comment = dc ? `${dc}\n${end.comment}` : end.comment;
                }
                this.doc.range[2] = end.offset;
                break;
            }
            default:
                this.errors.push(new errors.YAMLParseError(getErrorPos(token), 'UNEXPECTED_TOKEN', `Unsupported token ${token.type}`));
        }
    }
    /**
     * Call at end of input to yield any remaining document.
     *
     * @param forceDoc - If the stream contains no document, still emit a final document including any comments and directives that would be applied to a subsequent document.
     * @param endOffset - Should be set if `forceDoc` is also set, to set the document range end and to indicate errors correctly.
     */
    *end(forceDoc = false, endOffset = -1) {
        if (this.doc) {
            this.decorate(this.doc, true);
            yield this.doc;
            this.doc = null;
        }
        else if (forceDoc) {
            const opts = Object.assign({ _directives: this.directives }, this.options);
            const doc = new Document.Document(undefined, opts);
            if (this.atDirectives)
                this.onError(endOffset, 'MISSING_CHAR', 'Missing directives-end indicator line');
            doc.range = [0, endOffset, endOffset];
            this.decorate(doc, false);
            yield doc;
        }
    }
}

exports.Composer = Composer;
